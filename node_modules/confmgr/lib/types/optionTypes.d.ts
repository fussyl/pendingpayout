import { ConfigValue, Type } from './baseTypes';
export declare type RegexpWithAttributes = {
    pattern: RegExp;
    attributes: string;
};
export declare type ConfigItemOptions = {
    masked?: boolean;
    regexp?: RegExp | RegexpWithAttributes;
    mandatory?: boolean;
    default?: ConfigValue;
    type?: Type;
};
/**
 * Options you may pass to the `Print()` method.
 */
export declare type PrintOptions = {
    color?: boolean;
    compact?: boolean;
    logger?: (...args: any[]) => void;
};
